
# DATASOURCE
spring.datasource.url=jdbc:mysql://localhost:3306/jpa_one_to_one_relationship?useSSL=false
spring.datasource.username=root
spring.datasource.password=admin1919

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

# Hibernate ddl auto update listens for modifications, it
# does not delete existing tables though
spring.jpa.hibernate.ddl-auto = update

logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE